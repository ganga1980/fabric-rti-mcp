apiVersion: v1
kind: ServiceAccount
metadata:
  name: fabric-rti-mcp
  namespace: fabric-rti-mcp
  labels:
    app.kubernetes.io/name: fabric-rti-mcp
    app.kubernetes.io/component: rbac
  annotations:
    # This annotation is required for AKS workload identity
    # Replace with your actual user-assigned managed identity client ID
    # azure.workload.identity/client-id: "00000000-0000-0000-0000-000000000000"
